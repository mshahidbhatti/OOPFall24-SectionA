package week10_A.Lab;

import java.util.Arrays;

public class MediaManager {
    Movie movies[] = new Movie[100];

   // Arrays.sort(movies);

    // Arrays.sort(movies, new Comparator() {})
    public void sort(Movie movies){
      //  Arrays.sort(movies);
    }
    ;

}
